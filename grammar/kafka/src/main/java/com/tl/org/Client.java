package com.tl.org;

public class Client {

    public static void main(String[] args) {




//        AdminClient adminClient = AdminClient.create(properties);
//        AdminClient
//        ZkUtils zkUtils = ZkUtils.apply("localhost:2181", 30000, 30000, false);
//
//        //查询
//        Map<String, Properties> configMap = AdminUtils.fetchAllTopicConfigs(zkUtils).seq();
////        Set<String> topics = configMap.keySet().seq();
//
////        AdminClient.create()
//        AdminUtils.createTopic(zkUtils, "TLTopic", 1, 2, new Properties(), RackAwareMode.Enforced$.MODULE$);
//
//        //关闭
//        zkUtils.close();

//        Properties properties = new Properties();
//        properties.put(CommonClientConfigs.BOOTSTRAP_SERVERS_CONFIG, "localhost:9092,localhost:9093");
//        KafkaAdminClient adminClient = KafkaAdminClient.create(properties);


    }

}
